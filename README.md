Program aplikasi ini akan menghasilkan string yang urutan nya di balik menggunakan for.

Cara Menjalankan nya yaitu
Masukan string yang akan di balik kedalam "" contoh nya hello ini:

```bash
fmt.Println(reverseString("hello"))
```

Dengan menjalakan perintah berikut:

```bash
go run main.go
```
